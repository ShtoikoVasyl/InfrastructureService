spring:
  application:
    name: infrastructureservice
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
      name: infrastructureservice
      fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8008/eureka